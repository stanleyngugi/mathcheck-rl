[project]
name = "native-verify-seq"
description = "Finite-observation sequence tasks checked through an isolated shared Lean verifier"
tags = ["single-turn", "math", "lean", "verifiable-rewards", "procedural", "train", "eval"]
version = "0.2.0"
requires-python = ">=3.10"
dependencies = [
    "verifiers==0.3.0",
    "datasets>=3,<5",
    "native-verify>=0.1,<0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build]
include = ["native_verify_seq.py", "pyproject.toml"]

[tool.verifiers.eval]
num_examples = 20
rollouts_per_example = 4
