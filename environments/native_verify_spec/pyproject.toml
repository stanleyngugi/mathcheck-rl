[project]
name = "native-verify-spec"
description = "MathCheck RL answer-key-free bounded specifications and complete certificates"
tags = ["single-turn", "math", "lean", "verifiable-rewards", "answer-key-free"]
version = "0.1.0"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Stanley Ngugi" }]
dependencies = [
    "verifiers==0.3.0",
    "datasets>=3,<5",
    "native-verify>=0.2,<0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["native_verify_spec.py", "pyproject.toml"]

[tool.verifiers.eval]
num_examples = 16
rollouts_per_example = 4
